import type { ExportSettings, FilterSettings } from "@/types";

export class ExportService {
  // TODO: Phase 6 — export pipeline
  async export(
    image: ImageBitmap,
    settings: FilterSettings,
    exportSettings: ExportSettings,
  ): Promise<void> {
    void image;
    void settings;
    void exportSettings;
  }
}
